import ReferralPageBody from "@/components/ReferralBody";
import ScrollableContainer from "@/components/ScrollableContainer";
import React from "react";

const ReferralPageServer = () => {
  return (
    <ScrollableContainer>
      <ReferralPageBody />
    </ScrollableContainer>
  );
};

export default ReferralPageServer;
